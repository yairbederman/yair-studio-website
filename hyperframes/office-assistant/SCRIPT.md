# SCRIPT — what the film says

Subject: **the Managed AI Office Assistant running one office day**, with the
human approval gate where it belongs.

## The message arc

> Overnight inputs become a decided day: briefing, triage, documents, and
> follow-up are handled by the assistant — and everything that matters passes
> one human approval.

## On-frame text — three-source rule

Every word traces to `src/content/offers/ai-office-assistant.ts` (approved at
the phase-4 copy stop-point). Nothing else appears on frame.

| # | EN | HE | Traces to |
|---|---|---|---|
| 1 | Overnight inputs | קלט שנצבר בלילה | `example.map.nodes[0].label` |
| 2 | Morning briefing | תדריך בוקר | `build.items[0].title` |
| 3 | Email triage | מיון מיילים | `build.items[1].title` |
| 4 | Document workflows | תהליכי מסמכים | `build.items[2].title` |
| 5 | Follow-up and meetings | מעקב ופגישות | `build.items[3].title` |
| 6 | Human approval | אישור אנושי | `example.map.nodes[4].label` |
| 7 | The day starts decided | היום מתחיל מוכרע | `example.map.nodes[5].label` |

## The four streams (shape language, no text)

| Chip | Stream | Schematic content |
|---|---|---|
| 1 | Morning briefing | three hairlines + a copper tick (an assembled brief) |
| 2 | Email triage | small envelope glyph; one of three source envelopes lifts with a copper edge (the urgent one) before merging |
| 3 | Document workflows | a progress bar that **stalls mid-rail**, is surfaced (copper edge), then completes — stalled documents surfaced |
| 4 | Follow-up and meetings | a small open loop arc with a copper dash that closes it — the chase that no longer depends on memory |

## The peak — human approval

All four chips pause at the **same** copper gate on their way in (PRODUCT
principle 5: automation never runs end to end on its own). After the fourth
docks, the film holds: dim, one bounded ring bloom, "Human approval", ✓ draws,
and the four docked rows tick copper one by one — what approval releases.

## The payoff

"The day starts decided" — the offer's own closing node. The day panel holds
four resolved rows; settle for loop restart.

## Honesty rules (non-negotiable)

- No numbers, percentages, names, logos, or invented UI. Rows and chips are
  structural placeholders.
- Both locales carry the same story with their own approved copy — the HE
  render is a first-class variant, not a translation afterthought.
