# HyperFrames scratch & deps — keep out of commits
node_modules/
snapshots/
snapshots-en/
snapshots-he/
renders/
*.mp4
*.webm
*.mov

# HyperFrames boilerplate helper docs (regenerated by `init`) — local only
AGENTS.md
CLAUDE.md

# Review previews (regenerated from the master on demand)
render-check/
