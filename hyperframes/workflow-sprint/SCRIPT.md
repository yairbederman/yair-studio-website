# SCRIPT — what the film says

Subject: **the AI Workflow Sprint** — one stuck workflow in, three working
automations out, judgment kept human.

## The message arc

> One stuck workflow → mapped end to end → sorted into automatic /
> AI-assisted / human → three automations built → human approval → the
> workflow runs with less friction.

That is the offer page's own `example.map` sequence, animated.

## On-frame text — three-source rule

Every string traces to `src/content/offers/ai-workflow-sprint.ts` (line
numbers re-verified 2026-09-24; the site's film caption — not on frame — is
`film.caption` EN :28–29 · HE :203–204).

| # | EN | HE | Traces to | EN : HE line |
|---|---|---|---|---|
| 1 | One stuck workflow | תהליך אחד שנתקע | `example.map.nodes[0].label` | :103 · :278 |
| 2 | Mapped end to end | ממופה מקצה לקצה | `example.map.nodes[1].label` | :104 · :279 |
| 3 | Sorted | ממוין | `example.map.nodes[2].label` | :105 · :280 |
| 3a | Automatic | אוטומטי | `example.map.nodes[2].sub` (also hero lead, problems) | :105 · :280 |
| 3b | AI-assisted | בעזרת AI | `example.map.nodes[2].sub` | :105 · :280 |
| 3c | Human | אנושי | `example.map.nodes[2].sub` (also `human` section) | :105 · :280 |
| 4 | Three automations built | שלוש אוטומציות נבנות | `example.map.nodes[3].label` | :106 · :281 |
| 5 | Human approval | אישור אנושי | `example.map.nodes[4].label` | :107 · :282 |
| 6 | Workflow runs with less friction | התהליך רץ עם פחות חיכוך | `example.map.nodes[5].label` | :108 · :283 |

## Shape language (no text on tiles)

- **15 abstract tiles** = the workflow's steps. Scattered and slightly rotated
  at first (the stuck mess — one tile visibly sags); alignment = mapping;
  three rows = sorting; a copper edge + drawn check on three tiles = the three
  automations "clicking into place".
- **The human row stays copper-warm** from the sort onward — human work is
  not eliminated, it is named and protected.
- **The gate beat** reuses the office-assistant film's approval motif (filled
  copper node + white ✓) so the site's films share one approval language.

## Honesty rules (non-negotiable)

- Tiles carry no labels — no invented step names, tools, or metrics.
- Exactly three automation ticks — the offer promises three, the film shows
  three.
- Both locales carry the same story with their own approved copy.
