# SCRIPT — what the film says

Subject: the **LinkedIn Content Engine** — a managed pipeline that turns real
material into a steady presence, with the client's approval as the only gate.

## The message arc

> Your real material enters the engine. Angles are proposed, drafts are written in
> your voice, everything waits in one review queue — and nothing publishes without
> your approval. Then approved posts go out on a steady rhythm.

## The cast (all copy traces to `linkedin-content-engine.ts` — see DESIGN.md table)

| Element | Text | Source |
|---|---|---|
| Node 0 + sub | `Your material` · `calls · projects · wins` | `example.map.nodes[0]` |
| Node 1 | `Angles proposed` | `example.map.nodes[1]` |
| Node 2 | `Drafts in your voice` | `example.map.nodes[2]` |
| Node 3 | `Review queue` | `example.map.nodes[3]` |
| Node 4 (human) | `Your approval` | `example.map.nodes[4]` |
| Node 5 (out) | `Published on rhythm` | `example.map.nodes[5]` |
| Caption | `The approval point is yours` | `example.intro` |
| Closing | `A steady LinkedIn presence` | `hero.lead` |

## The peak — your approval

The signature beat. Everything upstream defocuses; the copper `Your approval`
checkpoint stays sharp. One item in the queue gets the human check. The caption
`The approval point is yours` appears verbatim. Publishing is downstream of the
human — nothing goes out on its own.

## The payoff — rhythm

The approved post flies out and becomes a cadence of evenly-spaced published beats:
`Published on rhythm`. The closing lands: `A steady LinkedIn presence`.

## Honesty rules (non-negotiable)

- Schematic node marks + abstract cards — no product UI, no metrics, no names.
- No "Sample data" chip (this is not a product screen); no neon/sparkles; one
  bounded copper ring at the peak.
- Nothing on frame that does not trace to the DESIGN.md source table.
